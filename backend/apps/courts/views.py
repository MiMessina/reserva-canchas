"""
Views — app courts

Sprint 0: placeholder.

Endpoints planificados (Sprint 1+):
  GET    /api/courts/                    — lista de canchas activas (staff + público)
  POST   /api/courts/                    — crear cancha (tenant_admin)
  GET    /api/courts/{id}/               — detalle de cancha
  PATCH  /api/courts/{id}/               — editar cancha (tenant_admin)
  DELETE /api/courts/{id}/               — soft-delete (tenant_admin)
  GET    /api/courts/{id}/availability/  — grilla de disponibilidad (público)
  GET    /api/schedule-blocks/           — horarios configurados (staff)
  POST   /api/schedule-blocks/           — crear horario (tenant_admin)
"""
