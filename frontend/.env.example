# Copiá este archivo a .env y ajustá los valores para tu entorno local.
# NUNCA commitees el .env con valores reales.

# URL base de la API (sin trailing slash).
# En desarrollo local (Docker Compose): http://localhost:8000/api
# En producción: https://tudominio.com/api
VITE_API_BASE_URL=http://localhost:8000/api
